# MSPR - ACME CRM database

Afin de lancer la database, lancer `docker compose -f compose-migrate-mariadb.yml up --build`.

Afin de stopper la database, lancer `docker compose -f compose-migrate-mariadb.yml down -v`.
